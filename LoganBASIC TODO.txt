[Bugs]
    - In Firefox, the background image of the Platformer example has a visible seem between its tiles.
    - The user-function callback for the timer in std_funcs module can interrupt another user-function that is waiting to finish executing.


[Features]
    - Standard functions for array searching.
    - Inline-If standard function.
    - Ability to include underscore characters in identifiers.
    - Keyboard shortcut for the Run button.

    - Ability of run() native function to return error messages instead of terminating the program.
    - Ability to run code in debugger.
    - A "deploy" mode to run a pre-compiled ObjUserFunc object from a JSON file.
    - Tutorial help files. 

    - Syntax highlighting.
    - Breakpoint display to debugger pane.
    - Variable display persistence between breakpoints in debugger.
    - Ability to display different sources in debugger.

    - Asynch option for native functions that download resources.
    - HTTP GET/POST native functions.
    - onError user function support.


[Refactors]
    - Dynamically add the usageRun and usageOpen links to all reference_doc help files in order to centralize the relative URLs they use.
    - Consolidate the request/result thread-message code from each component into the main modules.
    - Change the core modules into ES6 modules.
    - Extract the core modules into their own repo.
    - Change the component modules into ES6 modules.
